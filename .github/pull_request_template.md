## Summary

<!-- What does this change do? -->

## Checklist

- [ ] If I changed `examples/` or `scripts/validate_dsl.py`, I ran
      `python scripts/validate_dsl.py examples/*.yml` and it passed.
- [ ] I kept `README.md` and `README_CN.md` in parity.
- [ ] I did not add content from the All-Rights-Reserved upstream base without
      permission (see `NOTICE`).
